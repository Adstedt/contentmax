# Integration Management Center Configuration
# Copy this file to .env.local and fill in your values

# Google OAuth Configuration
# Get these from https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Encryption Key for storing credentials
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your_32_byte_hex_encryption_key_here

# Supabase Service Key (for server-side operations)
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_here

# App URL (for OAuth callbacks)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# OAuth Redirect URIs to configure in Google Console:
# - http://localhost:3000/api/auth/google/callback (for local development)
# - https://yourdomain.com/api/auth/google/callback (for production)

# Required Google APIs to enable in Google Cloud Console:
# - Google Analytics Data API
# - Google Search Console API
# - Content API for Shopping (for Merchant Center)

# Scopes required for each service:
# GA4: https://www.googleapis.com/auth/analytics.readonly
# GSC: https://www.googleapis.com/auth/webmasters.readonly
# GMC: https://www.googleapis.com/auth/content